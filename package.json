{
  "name": "cyberplayer-studio",
  "version": "2.0.0",
  "private": true,
  "description": "CyberPlayer STUDIO — bez-buildowy generator klipów audio-wizualnych (demorecorder.html) wraz z lekkim zestawem testów.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --test tests/*.test.mjs && node tests/check-syntax.mjs"
  }
}
